export const form = {
    auth:{
        login:{
            username:'input[placeholder="Username"]',
            password:'input[placeholder="Password"]',
            submitBtn:'button:has-text("Login")'
        }
    }
}